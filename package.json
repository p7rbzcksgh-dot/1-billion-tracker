{
  "name": "tcg-one-billion-monitor",
  "version": "1.9.1",
  "private": true,
  "description": "Railway milestone bug fix for the TCG Machines monitor, with self-service countdown thresholds, email, and Microsoft Teams alerts.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test-*.js",
    "check": "node --check server.js && node --check scraper.js && node --check client.js && npm test",
    "smoke": "node railway-smoke-test.js"
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "helmet": "8.3.0",
    "nodemailer": "9.0.3",
    "playwright": "1.61.0",
    "ws": "8.21.0"
  }
}
